function navigateToPage(pageUrl) {
    window.location.href = pageUrl; // Sayfayı yönlendirme
}
// Menü açma fonksiyonu
function showMenu() {
    document.getElementById("nav-links").style.right = "0";
}

// Menü kapama fonksiyonu
function hideMenu() {
    document.getElementById("nav-links").style.right = "-200px";
}

// Sayfa yönlendirme fonksiyonu
function navigateToPage(pageUrl) {
    window.location.href = pageUrl; // Sayfayı yönlendirme
}
